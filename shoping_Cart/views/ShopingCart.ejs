<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shoping Cart</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        @import url("https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css");
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .cart{
            position: relative;
            width: 5%;
            
        }
        .cart-icon{
            font-size: 1.5rem;
        }
        .badge{
            font-size: 0.7rem;
            position: absolute;
            top:0%;
            left: 0.4rem;
        }

        .cart-container{
            width: 100%;
            height: calc(100vh - 57px);
            display: flex;
            align-items: center;
            flex-direction: column;
            gap: 2%;
            background: linear-gradient(180deg,lightblue,skyblue);
        }
        .cart-row{
            width: 80%;
            height: 120px;
            display:flex;
            justify-content: space-around;
            align-items: center;
            background: rgba(166, 104, 228, 0.711);
            border: 1px solid red;
        }
        .cart-row img{
            width: 100px;
            height: 90%;
        }
        .cart-row .quantity{
            font-size: 1.8rem;
            color: white;
            font-family: serif;
        }
        .cart-row .price{
            font-size: 2rem;
            color: red;
            font-family: sans-serif;
            font-weight: bold;
        }
        .cart-row .delete,.minus{
            font-size: 1.6rem;
            cursor: pointer;
        }













        div.total-cart-row{
            width: 80%;
            height: 80px;
            display: flex;
            align-items: center;
            justify-content: space-around;
            background: lightcoral;
            color: white;
            font-weight: bolder;
            font-family: serif;
        }
        .total-items,.total-price{
            font-size: 2.5rem;
            color: red;
            font-family: serif;
        }
    </style>

</head>
<body>
    <%-include('./nav/navBar.ejs')%>
    <div class="cart-container">

  <%cart.forEach((val)=>{%>
   <div class="cart-row">
    <img src="<%=val.item.img%>" alt="">
    <div class="quantity">Quantity: <%=val.qty%></div>
    <div class="price"><%=val.price%>$</div>
    <div class="minus"><i class="bi bi-clipboard-minus text-danger"></i></div>
    <div class="delete"><a href=""><i class="bi bi-trash text-primary"></i></a></div>
   </div>

 <% })%>
   <div class="total-cart-row">
    <div class="total-items">Items:<%=session.cart.totalQty%></div>
    <div class="total-price">Total:<%=session.cart.totalPrice%>$</div>

   </div>

    </div>
</body>
</html>